#include <stdio.h>
#include <math.h>
int main(){
    int n;
    printf("Hay nhap so n \n");
    scanf("%d",&n);
    n = abs(n);
    printf("Gia tri tuyet doi cua so n la: %d ", n);
    return 0;

}