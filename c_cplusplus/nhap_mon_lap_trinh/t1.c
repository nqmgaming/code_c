#include <stdio.h>
#include <math.h>
int main() {
    int a,y;
    a=11;
    int b = ++a;
    int c = a++;
     
    printf("so b = %d, so c = %d, so a = %d", b,c, a);
    return 0;
}
    
